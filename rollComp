import java.util.*;

class rollComp implements Comparator{ //Compares the rollno variable
public int compare(Object object1, Object object2) {
	Student student1 = (Student)object1;
	Student student2 = (Student)object2;
	return Integer.compare(student1.rollno, student2.rollno);
	}
}
